# Packages

Shared workspace packages live here.

Planned packages:

- `ui` - shared UI components.
- `types` - shared TypeScript types.
- `sdk` - client SDKs.
- `shared` - common utilities.
- `prompts` - prompt templates and AI workflow assets.

